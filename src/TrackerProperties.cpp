#include "TrackerProperties.h"

TrackerProperties::TrackerProperties() :
    currentFramerate(0.0),
    hasLowblink(false),
    lowBlinkEnabled(false),
    unsupportedFirmware(false),
    currentIlluminationMode("")
{
}
